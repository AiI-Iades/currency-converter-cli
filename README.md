# Currency Converter CLI

Convert between USD, EUR, GBP, JPY, and CAD.

Usage: `currency-convert <amount> <from-currency> [<to-currency>]`

Examples:
`currency-convert 100 USD to EUR`
`currency-convert 50 GBP`